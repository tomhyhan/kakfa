// 1. Python Lint
// 2. package => docker image and push it to the docker hub
// 3. stage of my choice

// docker-compose file => image from docker hub account
// 
pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}